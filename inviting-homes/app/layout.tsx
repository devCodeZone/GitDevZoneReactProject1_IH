import type React from "react"
import { ThemeProvider } from "@/components/theme-provider"
import { Inter } from "next/font/google"
import "./globals.css"
import type { Metadata } from "next"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Inviting Homes | Premium Interior Design Solutions",
  description:
    "Transform your living spaces with Inviting Homes. We offer professional interior design services, 3D room visualization, and custom furniture solutions for your dream home.",
  keywords:
    "interior design, home decor, 3D room preview, custom furniture, living room design, kitchen renovation, home makeover, interior designer",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ThemeProvider attribute="class" defaultTheme="light" enableSystem disableTransitionOnChange>
          {children}
        </ThemeProvider>
      </body>
    </html>
  )
}



import './globals.css'